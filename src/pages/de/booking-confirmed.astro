---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<!doctype html>
<html lang="de">
  <head>
    <BaseHead
      title="Buchung bestätigt | UHL Systems"
      description="Dein Call mit UHL Systems ist bestätigt."
    />
    <meta name="robots" content="noindex, nofollow" />
    <style>
      .confirmed-hero {
        min-height: calc(100vh - 200px);
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .confirmed-card {
        text-align: center;
        max-width: 600px;
        margin: 0 auto;
      }

      .confirmed-headline {
        font-family: var(--font-display);
        font-size: clamp(2.5rem, 6vw, 4rem);
        font-weight: 800;
        letter-spacing: -0.02em;
        line-height: 1.1;
        color: var(--color-text);
      }

      .confirmed-subline {
        font-size: 1.25rem;
        color: var(--color-accent);
        margin-top: 0.5rem;
        font-weight: 500;
      }

      .confirmed-body {
        color: var(--color-muted);
        font-size: 1.05rem;
        line-height: 1.7;
        margin-top: 1.5rem;
      }

      .cta-primary {
        display: inline-block;
        margin-top: 2rem;
        padding: 0.85rem 2rem;
        background: var(--color-accent);
        color: #080808;
        font-weight: 700;
        border-radius: 8px;
        text-decoration: none;
        transition: box-shadow 0.2s ease, transform 0.2s ease;
      }
      .cta-primary:hover {
        box-shadow: 0 0 20px color-mix(in srgb, var(--color-accent) 40%, transparent);
        transform: translateY(-1px);
      }

      .aside-note {
        margin-top: 2.5rem;
        font-size: 0.9rem;
        color: var(--color-muted-dark);
      }
      .aside-note a {
        color: var(--color-accent2);
        text-decoration: underline;
        text-underline-offset: 3px;
      }
      .aside-note a:hover {
        color: var(--color-text);
      }

      .fade-in {
        opacity: 0;
        transform: translateY(16px);
        animation: fadeIn 0.7s ease-out forwards;
      }
      .fade-in-delay-1 { animation-delay: 0.15s; }
      .fade-in-delay-2 { animation-delay: 0.3s; }
      .fade-in-delay-3 { animation-delay: 0.45s; }

      @keyframes fadeIn {
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>
  <body>
    <Header />
    <main class="confirmed-hero px-6">
      <div class="confirmed-card">
        <h1 class="confirmed-headline fade-in">Du bist gebucht. 🔥</h1>
        <p class="confirmed-subline fade-in fade-in-delay-1">Wir sehen uns bald.</p>
        <p class="confirmed-body fade-in fade-in-delay-2">
          Dein Call ist bestätigt. Schau in dein E-Mail-Postfach für die Kalendereinladung.
          Wir schauen uns deine Antworten vorher an, damit wir die gemeinsame Zeit optimal nutzen können.
        </p>
        <a href="/de/" class="cta-primary fade-in fade-in-delay-3">Zurück zur Startseite</a>
        <p class="aside-note fade-in fade-in-delay-3">
          Schon mal reinlesen - <a href="/services/audit">so funktioniert die UHL Revenue Leak Map.</a>
        </p>
      </div>
    </main>
    <Footer />
  </body>
</html>
